# checklist.md — 남은 태스크 큐

우선순위대로. 항목 하나를 in_progress로 옮기고 착수, 완료하면 체크. 완료 후 곧바로 다음 항목.

## 다음 세션에서 이어할 항목


- [ ] **③ 홈 화면 인사이트 배너**
  - 위치 후보: `#highlights` 아래 or `#charts` 위. 대학 3~4개 미리보기 카드.
  - 목적: 상단 "변화 인사이트" 버튼을 눌러야만 발견 가능한 현 구조 개선
  - 카드 클릭 → 해당 대학 인사이트 오버레이 오픈
  - 성공 조건: 홈 첫 진입에서 인사이트 존재 인지 가능

- [ ] **④ 모바일 반응형 점검·수정**
  - 뷰포트: 375×812(iPhone), 768(iPad)
  - 점검 대상: 사이드바 오버레이, 유불리 카드 그리드, 전형 목록 가로 스크롤, 인사이트 오버레이 (좌우 레일 → 상하 스택), 입결 컷 필터 UI, 유불리 판정 기준 범례
  - 성공 조건: 두 뷰포트에서 body 가로 스크롤 0, 주요 버튼 44px 터치 타깃 확보, 텍스트 잘림 없음

- [ ] **⑤ verify_data pre-commit hook 자동화**
  - `.git/hooks/pre-commit` 에 `data.js` 또는 `build_data.py` 스테이지된 경우 `python3 verify_data.py` 실행 → 실패 시 커밋 차단
  - 스크립트 자체는 리포에 `dashboard/hooks/pre-commit` 로 두고, README/DATA_UPDATE.md 에 설치 명령(`cp dashboard/hooks/pre-commit .git/hooks/ && chmod +x .git/hooks/pre-commit`) 명시
  - 성공 조건: 일부러 (외) 잔존한 파일 커밋 시 실패로 차단됨

## 잠재 후보 (백로그, 요청 시)
- 대시보드 내부 AI 상담 도우미(성적 입력 → 유불리 자동 분석). 큰 작업, 별도 세션.
- 인사이트 대학을 42교 → 확장 지속. 넥스트플레이 '2027 VS 2026' 시리즈 잔여 대학·후속 발행분 확인해 추가.
- 지원카드·비교함 공유 URL 생성.

## 완료 이력
- ✅ 인사이트 대학 확장 32교 → 42교 (거점국립대 8교 + 아주대·숙명여대)
- ✅ V6.29 데이터 반영
- ✅ FOUC(테마 깜빡임) 제거
- ✅ 유불리 판정 기준 범례
- ✅ 데이터 검증 하네스(`verify_data.py` + `DATA_UPDATE.md`)
- ✅ (외) → 정원 외 채용조건형 배지 통일
- ✅ 인사이트 문구 두괄식·개조식 렌더
- ✅ 입결 컷 등급 필터(평균/70%/90% + 슬라이더, "X.X 이내")
- ✅ 세션 핸드오프 아티팩트(이 파일 포함)
